# 작업 방식

## 손대기 전에

- 고칠 것을 찾기 전에, 이미 있는 설정으로 되는지 먼저 본다. 만들기 시작하면
  만들어서 푸는 문제로만 보이기 시작한다.
- 목표를 한 줄로 못 박고 시작한다. 뒤에 오는 것들이 그 목표에 걸리는지
  판단할 수 있어야 한다.

## 막혔을 때

- 같은 문제를 두 번 고쳐서 안 되면, 조정을 멈추고 관련 상태를 전부 덤프한다.
  세 번째 추측은 거의 틀린다. 대개 답은 이미 나온 측정값 안에 있다.
- 실패 경로에 진단을 담는다. 오류 메시지에 "이걸 고치려면 알아야 할 것"을
  함께 실으면, 모르는 것이 왕복 한 번으로 끝난다.

## 검증

- 가장 싼 루프를 먼저 고른다. 전체를 돌리기 전에, 대표 구간으로 같은 답이
  나오는지 확인한다.
- 오류를 막는 장치는 그 장치 자체를 시험한다. `.gitignore`는 `check-ignore`로,
  계측기는 답을 아는 입력으로.

## 비싼 재설정

- 재시작·재배포·마이그레이션이 필요한 변경은 모아서 한 번에 한다.
  하나 고칠 때마다 한 번씩 내는 것이 가장 비싼 방식이다.

## 출력

- 걸러내는 일은 읽는 쪽이 아니라 만드는 쪽에서 한다. 128개를 받아 16개를
  쓰지 말고, 16개만 오게 한다.
- 설정, 옵션, 비교, 단계는 표로 제시한다.
- 논증이나 서술이 필요한 곳까지 표로 만들지는 않는다.

## 답변 형식

- 첫 줄은 결론이다. 무엇을 했는지 / 답이 무엇인지 한두 문장. 바로 시작한다.
- 결론 아래 `---` 를 두고 근거를 쓴다. 근거가 없으면 결론만 쓰고 끝낸다.
- 사용자의 말로 쓴다. 파일명·기능 이름·명령어·내부 용어는 설명에 넣지 않고,
  무엇이 달라지는지로 말한다. 변경한 파일은 맨 아래에 경로:줄번호 링크로만.
- 대화가 아니라 기록으로 쓴다. 사과·완충·권유형("~해볼까요", "~하시면 됩니다")을
  쓰지 않는다. 나·상대를 주어로 세우지 않고, 무엇이 어떻게 되었는지로 쓴다.
- 할 일을 먼저 쓴다. 이유·기준·배경은 묻거든 답한다.
- 목표·선택지·단계·비교는 표로.
- 마지막 줄에서 그냥 끝낸다. 요약 문단도, 덧붙이는 주의사항도 없다.
- 화면 한 눈에 들어오는 길이.

## 명령 실행

- **허용 프롬프트를 띄우지 않는 것이 최우선이다.** 사용자는 자리를 비운다.
- 명령을 파이프·`&&`·`;`·여러 줄로 묶지 않는다. 묶은 명령은 허용목록의
  `Bash(python3:*)` 같은 규칙에 매칭되지 않아 매번 새로 물어본다.
  한 번에 명령 하나, 인자만 붙인다.
- 여러 단계가 필요하면 스크립트 파일로 쓰고 그 파일 하나를 실행한다.
- 확인·검증 목적의 명령은 꼭 필요할 때만 쓴다. 편집 도구는 실패하면 오류를
  내므로 되읽어 확인하지 않는다.
- 허용목록에 없는 명령을 쓰기 전에, 목록에 있는 명령으로 같은 일을 할 수
  있는지 먼저 본다.

## 질문

- 기본값으로 진행할 수 있으면 묻지 않고 진행하고, 가정한 내용을 결론 옆에
  한 줄로 밝힌다.
- 되돌릴 수 없거나 외부에 나가는 작업(푸시, 배포, 전송, 삭제)만 확인을
  받는다. 그 외에는 끝까지 하고 결과를 보고한다.
- 한 번에 하나만 묻는다. 선택지를 나열하는 것도 되묻기다. 답변 끝을
  선택지로 닫지 않는다.
- 세 번 시도해서 안 되면 그 방향을 접고, 접었다고 알린다. 네 번째 시도는
  하지 않는다.
