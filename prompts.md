# 프롬프트 모음

## 1. 새 맥에 설정 적용 (한 번만)

```
https://github.com/inbm001/claude-setup 를 clone 해서 README 대로 적용해줘.
CLAUDE.md 는 ~/.claude/ 에 놓고, permissions 는 기존 settings.json 에 병합.
경로의 inbm_mini 는 이 맥 사용자명으로 바꿔줘. 플러그인 2개도 설치.
끝나면 Claude 재시작해야 한다고 알려줘.
```

## 2. 새 프로젝트 시작

입력창 하단 모드를 **Plan** 으로 바꾸고 붙여넣는다.

```
새 프로젝트를 시작한다. 지금은 브레인스토밍 단계다.

[만들려는 것]
(여기에 한두 줄)

[진행 방식]
- 코드도 파일도 아직 만들지 마라. 계획이 확정될 때까지 Plan 모드다.
- 먼저 내 구상의 빈 곳을 짚어줘. 결정이 필요한 것들을 /ask-in-bulk 로
  한 번에 물어라. 하나씩 되묻지 마라.
- 답을 받으면 계획을 세워라. 각 단계마다 "무엇을 만들고 어떻게 확인하는지"
  한 줄씩.
- 기술 선택은 논의부터 하고 그다음에

계획이 확정되면 내가 Accept Edits 로 바꾸겠다. 그 전엔 쓰지 마라.
p.s : 반말금지
```

계획 승인 후에는 "시작해" 한마디면 된다.

## 단계별 도구

| 단계 | 도구 |
|---|---|
| 브레인스토밍 | 그냥 대화 |
| 질문이 쌓일 때 | `/ask-in-bulk` — 여러 질문을 한 폼으로 묶어 한 번에 답변 |
| 결과 정리 | Artifact — 링크로 남겨 다음 세션에 이어씀 |
| 계획 | Plan 모드 (입력창 하단) |
| 큰 설계 | `Plan` 서브에이전트 |
| 화면 있는 프로젝트 | `/design` — 캔버스 목업 |
| 코딩 | `/ponytail` — 안 만드는 쪽을 먼저 검토 |
| 원인 불명 버그 | 고치기 전에 증거부터. 두 번 고쳐 안 되면 상태를 전부 덤프 |
| 프로젝트 규칙 | `/init` — 코드 생긴 뒤 CLAUDE.md 자동 생성 |
| 검토 | `/code-review` |

## 순서

```
1. 새 폴더에서 대화 시작 → 아이디어 정리
2. /ask-in-bulk 로 결정사항 한 번에 확정
3. Plan 모드 → 계획 확정 → 승인
4. Accept Edits 로 전환 → 코딩
5. /init 으로 규칙 고정
```
